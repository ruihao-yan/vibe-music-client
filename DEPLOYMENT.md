# Vibe Music Client 部署流程文档

本文档描述从项目初始化到部署的完整流程。

## 1. 环境准备

### 1.1 安装必要的开发工具
- 安装 Node.js (版本 >= 18.12.0)
- 安装 pnpm 包管理器 (版本 >= 7)
- 安装 Git 版本控制工具

### 1.2 准备后端服务
- 部署并启动 Vibe Music Server 后端服务
- 记录后端服务的 API 地址和端口

## 2. 项目初始化

### 2.1 克隆项目代码
- 从 GitHub 克隆项目仓库到本地
- 进入项目目录

### 2.2 安装项目依赖
- 使用 pnpm 安装所有项目依赖包

### 2.3 配置环境变量
- 复制 `.env.development` 文件为 `.env.development.local`
- 配置开发环境的后端 API 地址 (`VITE_APP_BASE_API`)
- 根据需要配置路由模式和公共基础路径

## 3. 开发阶段

### 3.1 启动开发服务器
- 运行开发命令启动本地开发服务器
- 默认端口为 8090
- 在浏览器中访问本地开发地址

### 3.2 代码质量检查
- 运行 ESLint 检查代码规范
- 使用 Prettier 格式化代码
- 执行 TypeScript 类型检查

### 3.3 开发调试
- 进行功能开发和调试
- 确保前后端 API 对接正常
- 测试各项功能是否正常工作

## 4. 测试环境

### 4.1 配置测试环境
- 复制 `.env.test` 文件为 `.env.test.local`
- 配置测试环境的后端 API 地址

### 4.2 构建测试版本
- 使用测试环境模式构建项目
- 检查构建输出是否正常

### 4.3 预览测试构建
- 运行测试环境预览命令
- 验证测试环境构建结果

## 5. 生产环境准备

### 5.1 配置生产环境变量
- 复制 `.env.production` 文件为 `.env.production.local`
- 配置生产环境的后端 API 地址
- 确认路由模式和公共路径配置正确
- 检查所有环境变量配置无误

### 5.2 代码最终检查
- 执行完整的代码规范检查
- 修复所有 ESLint 和 TypeScript 错误
- 格式化所有代码文件
- 确保没有调试代码或敏感信息

### 5.3 构建生产版本
- 使用生产环境模式构建项目
- 验证构建输出目录 (默认为 `dist`)
- 检查打包后的静态资源文件

### 5.4 本地预览生产构建
- 运行生产环境预览命令
- 全面测试生产版本功能
- 确认所有功能正常运行

## 6. 部署

### 6.1 部署到 Netlify (推荐)

#### 6.1.1 配置 Netlify
- 在 Netlify 创建新项目
- 连接 GitHub 仓库
- 配置构建命令：`pnpm build`
- 配置发布目录：`dist`
- 配置环境变量 `VITE_APP_BASE_API`

#### 6.1.2 部署设置
- 检查 `netlify.toml` 配置文件
- 确认重定向规则配置正确
- 启动自动部署

#### 6.1.3 验证部署
- 访问 Netlify 提供的域名
- 测试所有功能是否正常
- 检查 API 请求是否成功

### 6.2 部署到其他静态托管平台

#### 6.2.1 Vercel 部署
- 导入 GitHub 项目到 Vercel
- 配置构建命令和输出目录
- 设置环境变量
- 触发部署

#### 6.2.2 GitHub Pages 部署
- 修改 `vite.config.ts` 中的 `base` 配置
- 构建项目
- 推送 `dist` 目录到 `gh-pages` 分支
- 在仓库设置中启用 GitHub Pages

#### 6.2.3 自有服务器部署
- 使用 Nginx 或 Apache 作为 Web 服务器
- 将构建后的 `dist` 目录上传到服务器
- 配置服务器路由重定向规则
- 配置 HTTPS 证书
- 配置反向代理 (如需要)

## 7. 部署后验证

### 7.1 功能测试
- 测试用户注册和登录功能
- 验证音乐播放功能
- 检查歌曲搜索功能
- 测试评论和互动功能
- 验证个人信息管理功能
- 检查收藏和下载功能

### 7.2 性能检查
- 检查页面加载速度
- 验证资源加载是否正常
- 测试音频播放性能
- 检查移动端适配情况

### 7.3 浏览器兼容性测试
- 测试主流浏览器兼容性
- 验证不同设备上的显示效果
- 检查响应式布局

## 8. 监控和维护

### 8.1 建立监控机制
- 配置错误日志收集
- 设置性能监控
- 监控 API 调用情况

### 8.2 定期维护
- 定期更新依赖包版本
- 修复发现的问题
- 优化性能和用户体验
- 更新功能和内容

### 8.3 备份和回滚
- 保存每次部署的构建产物
- 准备回滚方案
- 记录部署历史和变更日志

## 9. 常见问题处理

### 9.1 构建失败
- 检查 Node.js 和 pnpm 版本
- 清除缓存并重新安装依赖
- 检查代码语法错误
- 查看构建日志定位问题

### 9.2 部署后 API 无法访问
- 检查环境变量配置
- 确认后端服务运行状态
- 检查 CORS 跨域配置
- 验证网络连接和防火墙设置

### 9.3 路由 404 错误
- 检查服务器重定向规则配置
- 确认路由模式设置正确
- 验证 `base` 配置是否匹配部署路径

### 9.4 资源加载失败
- 检查静态资源路径配置
- 验证 CDN 配置 (如使用)
- 检查服务器 MIME 类型配置
- 确认文件上传完整性

## 10. 附录

### 10.1 常用命令参考
- `pnpm install` - 安装依赖
- `pnpm dev` - 启动开发服务器
- `pnpm build` - 构建生产版本
- `pnpm preview` - 预览生产构建
- `pnpm build:test` - 构建测试版本
- `pnpm preview:test` - 预览测试构建
- `pnpm lint` - 代码规范检查
- `pnpm lint:fix` - 自动修复代码规范
- `pnpm format` - 格式化代码
- `pnpm type-check` - TypeScript 类型检查

### 10.2 重要文件说明
- `.env.development` - 开发环境配置
- `.env.production` - 生产环境配置
- `.env.test` - 测试环境配置
- `vite.config.ts` - Vite 构建配置
- `netlify.toml` - Netlify 部署配置
- `package.json` - 项目依赖和脚本

### 10.3 相关链接
- [项目仓库](https://github.com/Alex-LiSun/vibe-music-client)
- [后端服务](https://github.com/Alex-LiSun/vibe-music-server)
- [Vue 3 文档](https://vuejs.org/)
- [Vite 文档](https://vitejs.dev/)
- [Netlify 文档](https://docs.netlify.com/)
